/**
 * API configuration — resolves the backend base URL.
 *
 * We are now using Vercel Serverless Functions directly (which trigger GitHub Actions).
 * API_BASE is empty to use the same-origin URL.
 */
export const API_BASE = '';
